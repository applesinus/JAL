package main

func main() {
	/*string_to_test := "5*5*10/25"

	line, err := assign(0, string_to_test)
	fmt.Printf("%v => %v\n", line, err)

	fmt.Printf("\n\nPress any key to exit")
	scanner := bufio.NewScanner(os.Stdin)
	scanner.Scan()*/
}
